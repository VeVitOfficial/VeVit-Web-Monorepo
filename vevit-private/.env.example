# VeVit — local environment template.
# Copy to .env.local (or home.env.local on the server) and fill in real values.
# Never commit real values — .env.* is gitignored.

# Gemini API key used by the Edu AI proxy (edu/legacy + edu/ai-gramotnost).
GEMINI_API_KEY=